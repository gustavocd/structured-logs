# Exploring slog package

A set of examples where we explore the new package `slog` for logging in Go.

## Running the examples

```bash
make help
```

> **NOTE**: You need to have `make` installed in your system.
